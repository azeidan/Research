package org.cusp.bdi.ds.test

import org.cusp.bdi.ds.bt.AVLTree
import org.cusp.bdi.ds.geom.Point

import scala.collection.mutable.ListBuffer

object TestAVL {

  def main(args: Array[String]): Unit = {

//    val l = ListBuffer[Int](9,99,999)
//
//    val abc=  l.toStream
//abc.foreach(println)
//    val iterIdx1 = ListBuffer(3166, 4040, 1924, 7823, 8314, 5288, 8829, 6801, 4944, 3287, 5403, 4001, 2794, 7992, 1239, 2706, 4542, 2137, 8327, 3890, 4903, 1354, 8213)
//    val iterIdx2 = ListBuffer(15, 81, 28, 38, 8, 86, 89, 33, 72, 50, 11, 8, 53, 24, 78, 99, 14, 70, 53, 33, 3, 47, 6)

//    iterIdx1.foreach(idxX => {
//
//      val btNode = bt.getOrElseInsert(idxX)
//
//      if (btNode.data == null)
//        btNode.data = new AVLTree[ListBuffer[Point]]()
//
//      iterIdx2.foreach(idxY => {
//
//        val btNode2 = btNode.data.getOrElseInsert(idxY)
//
//        if (btNode2.data == null)
//          btNode2.data = new ListBuffer[Point]
//
//        btNode2.data += new Point(idxX.toDouble, idxY.toDouble)
//      })
//    })
//
//    //    bt.foreach(println)
//
//    bt.inOrder().foreach(node => {
//
//      val lstSubtreeNodes = node.data.inOrder()
//
//      printf("%s%n\t[%s]%n", node.keyValue, lstSubtreeNodes.map(_.keyValue).mkString(","))
//    })
  }
}
